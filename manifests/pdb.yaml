apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: autodeployaksapplication
  labels:
    app.kubernetes.io/name: autodeployaksapplication
    app.kubernetes.io/part-of: autodeployaksapplication-project
    kubernetes.azure.com/generator: devhub
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: autodeployaksapplication